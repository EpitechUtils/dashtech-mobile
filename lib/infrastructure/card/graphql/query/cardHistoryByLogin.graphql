query CardHistoryByLogin($email: String!) {
    cardHistoryByLogin(email: $email) {
        createdAt
        eventType
        message
        profile {
            id
            email
        }
    }
}
