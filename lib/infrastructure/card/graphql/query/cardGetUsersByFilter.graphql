query CardGetUsersByFilters($input: PromoFetchInput!) {
    cardGetUsersByFilters(filters: $input) {
        login
        title
        location
        picture
        card {
            id
            nfcTag
        }
    }
}
